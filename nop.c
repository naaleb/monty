#include "monty.h"
/**
  *f_nop: Doesn’t do anything
  *@head: Pointer to stack head
  *@counter: line number parameter
  *Return: no return
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
